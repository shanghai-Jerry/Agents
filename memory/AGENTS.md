# Agent Memory

This file is loaded by MemoryMiddleware at agent startup and injected into the
system prompt. Use it to store project-level conventions, preferences, and
context that should persist across all conversations.